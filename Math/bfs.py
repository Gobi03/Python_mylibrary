import queue

que = queue.Queue()
que.put(i)  # 初期値
while(not que.empty()):
    now = que.get()
    
    ## process to now

    # enqueue
    for i in range():
        nex = i-th statement
        if(condition):
            que.put(nex)
